// Generated with util/create-component.js
import React from "react";
import { Icon, IconProps } from "@iconify/react";

import "./Icon.css";

const IconifyIcon = ({ icon, ...rest }: IconProps) => (
  <Icon icon={icon} className="icon" {...rest} />
);

export default IconifyIcon;
